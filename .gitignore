.DS_Store
.context/

# add to skip-worktree to ignore local changes
.vscode/settings.json

# for ipc files (inter process communication)
app/renderer

# for deployment
deployment/upload_api.key

# Obsidian per-user workspace state (vault root is thegates/)
/.obsidian/
